//el nombre de los campos deben coincidir con los del origen en 
//observable observable [campos de una list en spring desde un controller]
export class VoteCounter{
    idHero!: string;
    alias!:string;
    votes!:number;
}